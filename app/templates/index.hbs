{{#with-anything
  user="service:user"
  session="service:session"
  config="service:config"
  router="router:main"
  as |_|
}}
  {{#if _.session.isAuthenticated}}
    Hello {{_.user.name}}
    You're currently at {{_.router.currentURL}},<br>
    which is actually on the {{_.router.currentRouteName}} route.
    {{#if _.user.isAdmin}}
      <br><br><br>
      Psst. You're an admin so I can let you in on a little secret.<br>
      Your app's module prefix is: {{_.config.modulePrefix}}
    {{/if}}
  {{else}}
    Sorry, you're actually logged out.
  {{/if}}
{{/with-anything}}
